{
  "name": "ops_resource_creation_tool",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf --glob dist/*",
		"build": "pnpm run clean && tsc -b ./tsconfig.build.json && cp appsscript.json dist/ && cp src/*.html dist/",
		"push": "pnpm run build && pnpm clasp push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@11.1.1",
  "devDependencies": {
    "@google/clasp": "^3.3.0",
    "@types/google-apps-script": "^2.0.8",
    "@types/node": "^25.6.0",
    "globals": "^17.6.0",
    "rimraf": "^6.1.3",
    "tgas-local": "^1.1.1",
    "typescript": "^6.0.3",
    "typescript-plugin-tgas-local": "^0.0.3"
  }
}
